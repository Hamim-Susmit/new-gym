# Database

## Schema setup
1. Create a Supabase project or Postgres database.
2. Run `schema.sql` to create tables.
3. Run `seed.sql` to insert sample data.

## Migrations
- `migrations/001_initial.sql` applies the base schema.
